#!/usr/bin/lua

local fs = require 'nixio.fs'
local site = require 'gluon.site_config'

local step_size = site.respondd_workaround.step_size

local f = io.open('/usr/lib/micron.d/respondd-workaround', 'w')
f:write(string.format('*/%i * * * * /usr/sbin/respondd-workaround\n', step_size))
f:close()
